model User {
    id            Int    @id @default(autoincrement())
    username      String @unique
    email         String @unique
    password_hash String
    deletedAt     DateTime?
    role String @default("USER") 
    
    profile   Profile?

    orders Order[]

    @@map("users")
}
