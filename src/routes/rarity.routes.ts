// import { Router } from "express";
// import { validate } from "../utils/validator";

// import {
//   created,
//   deleted,
//   getAll,
//   getById,
//   search,
//   updated,
// } from "../controller/rarity.controller";
// import {
//   createRarityValidation,
//   getRarityByIdValidation,
//   updateRarityValidation,
// } from "../middleware/rarity.validation";

// const router = Router();

// router.get("/", getAll);

// router.get("/search", search);

// router.get("/:id", validate(getRarityByIdValidation), getById);

// router.post("/", validate(createRarityValidation), created);

// router.put("/:id", validate(updateRarityValidation), updated);

// router.delete("/:id", deleted);

// export default router;
