// import { Router } from "express";
// import { validate } from "../utils/validator";

// import {
//   created,
//   deleted,
//   getAll,
//   getById,
//   search,
//   updated,
// } from "../controller/element.controller";
// import {
//   createElementValidation,
//   getElementByIdValidation,
//   updateElementValidation,
// } from "../middleware/element.validation";

// const router = Router();

// router.get("/", getAll);

// router.get("/search", search);

// router.get("/:id", validate(getElementByIdValidation), getById);

// router.post("/", validate(createElementValidation), created);

// router.put("/:id", validate(updateElementValidation), updated);

// router.delete("/:id", deleted);

// export default router;
